export interface CalendarEvent {
  title: string;
  activity_date: string;
  start: string;
  end: string;
  state_name: string;
  user_names: string;
  user_abb: string;
  model_uuid: string;
  model_type: string;
  color?: string;
}
